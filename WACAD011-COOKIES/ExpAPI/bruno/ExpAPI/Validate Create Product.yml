info:
  name: Validate Create Product
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:4455/v1/product
  body:
    type: json
    data: |-
      {
        "name": "A",
        "price": -10,
        "stock": -5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
