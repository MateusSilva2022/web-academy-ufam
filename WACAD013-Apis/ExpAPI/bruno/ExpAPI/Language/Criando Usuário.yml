info:
  name: Criando Usuário
  type: http
  seq: 16

http:
  method: POST
  url: http://localhost:4455/v1/auth/login
  body:
    type: json
    data: |-
      {
        "email": "mateus@email.com",
        "senha": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
