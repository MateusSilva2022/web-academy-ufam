info:
  name: login
  type: http
  seq: 19

http:
  method: POST
  url: http://localhost:4455/v1/auth/login
  body:
    type: json
    data: |-
      {
        "email": "mateus2@email.com",
        "senha": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
